package ui

// ShowAgentSettings shows the agent editor dialog.
func ShowAgentSettings(m *Main) {
	// var agent *llm.Agent
	// lastEditedAgent := m.app.Preferences().IntWithFallback("agent.last-edited", 0)
}
